
| 24 | ✅ | Flonum fast paths | The two-argument arithmetic and comparison expansions take a second fast path when both operands are JavaScript numbers, for which the JavaScript operator is what the tower computes (`=` as `===` agrees on `-0.0` and NaN); exact integers are tested first and cost what they did. Compiled tier `flonum` 7.51x against the task-23 build (`mbrot` 58x, `sumfp` 32x, `fibfp` 23x), no other class moved; against the interpreter the class went from 12.6x to 98x. `fibfp` is now 94% its own generated code; what holds back the rest is vector access (25) and, in `mbrotZ`, complex arithmetic, which stays in the tower. Mixed exactness still takes the tower, deliberately. | R71 |
