
| 25 | ✅ | Inline vector access | Ceiling first: unchecked array access was worth 1.4–2.1x on the vector-heavy programs. The planned expansion, the checks inline like `car`'s, was slower than the primitive in isolation and caught little of that, since comparing a `bigint` index costs more than converting it (R72). What caught nearly all of it: the expansion calls a runtime helper, `R.vectorRef`/`R.vectorSet`, which converts the index once, handles an array and an exact index in range, and passes everything else -- every error included -- to the primitive; `vector-length` is inline. Suite, compiled: `flonum` 1.26x (`simplex` 1.82x), `vector` 1.18x (`array1` 1.38x), `list` 1.07x (`earley` 1.34x), nothing else moved; interpreter unchanged. Against the interpreter, `vector` 35x and `flonum` 131x. The same helper would suit `string-ref` and `bytevector-u8-ref`, unmeasured. | R72 |
| 26 | ✅ | Tail calls between procedures | Ceiling first: every tail call made directly, unsoundly, was worth 1.2-1.4x on six programs -- and overflowed `cpstak` and `fft`, and made `fibc` 2.6x slower, since a continuation called directly throws (R73). Shipped: a tail call to a compiled procedure or a primitive is a JavaScript call while the stack direct tail calls hold is under a budget, `R.tailStack`; each site charges its own frame's size, known at compile time, and gives it back in a `finally`; past the budget, and for any other callee, a `TailCall` as before, now through `R.tailCall`, which also reports a non-procedure as the interpreter does instead of "ctl.step is not a function". A limit in calls rather than stack, or one per chain, measured unsafe (R73). Suite, compiled, best of three alternated: `vector` 1.12x (`bv2string` 1.25x), `continuation` 1.11x (`dynamic` 1.28x), `call` 1.09x (`cpstak` 1.35x), `list` 1.07x (`graphs` 1.22x, `earley` and `peval` 1.18x, `mazefun` 1.16x), nothing slower than 0.98x; interpreter unchanged. One tail call 20.9 → 1.9 ns. `earley` recurses deeply enough to spend the budget and gets 1.2x of a 1.4x ceiling. Generated code 4.5-6% larger. Found on the way: 27 and 28 (R74). | R73, R74 |
