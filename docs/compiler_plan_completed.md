
| 22 | ✅ | Code generation, as Scheme passes over the IR: the global read and the call path | Measured first, as R61 did. A global read was a hash lookup on every reference, worth up to 1.5x as a ceiling; it is now a cell that the frame holding the name keeps current through every write, read as `(C.v ?? G())`. With the runtime values every call site uses read once per procedure: compiled tier `call` 1.63–1.67x, `flonum` 1.25x, `list` 1.20x, `vector` 1.14–1.24x, `fixnum` 1.09–1.17x, `continuation` 1.10x; `bignum` and `string` unchanged; the interpreter unchanged; the compiler's own lowering 66 → 51 ms a pass. The raw-call lookup on every call costs nothing measurable (R66). Found on the way: a prebuilt table generated against an older runtime interface made the build write empty tables and report success (R67); tables now record the interface. Generated code grew 8%. The lowering tests in `loop_compilation_tests.js` moved to `tests/compiler/loop_tests.scm`. | R66, R67 |
